[project]
name = "ai-mock"
version = "0.1.0"
description = "Q-Feed AI mock server for load and integration testing"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.8.0",
  "python-multipart>=0.0.9",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
